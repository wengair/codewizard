<div class="filter">
  <div class="show-border">
    <p id='close' style="text-align: right;cursor: pointer;">close</p>
    <h1>Select a level</h1>
    <% levels.each do |level| %>
      <%= link_to level.name, level_path(level.questions[0]) %>
    <% end %>
  </div>
</div>
